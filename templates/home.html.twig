<!doctype html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Bienvenido | Mi Aplicación</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Animación de fondo */
        @keyframes gradientBG {
            0% { background-color: #6a11cb; }
            25% { background-color: #2575fc; }
            50% { background-color: #ff416c; }
            75% { background-color: #ff4b2b; }
            100% { background-color: #6a11cb; }
        }

        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .landing {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            animation: gradientBG 15s ease infinite;
            transition: background-color 2s;
            color: #fff;
            text-align: center;
            padding: 0 1rem;
        }

        .landing h1 {
            font-size: 3rem;
            font-weight: 700;
            text-shadow: 2px 2px 8px rgba(0,0,0,0.4);
            margin-bottom: 1rem;
        }

        .landing p {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            text-shadow: 1px 1px 6px rgba(0,0,0,0.3);
        }

        .btn-panel {
            font-size: 1.5rem;
            padding: 1rem 2.5rem;
            border-radius: 0.75rem;
            box-shadow: 0 8px 15px rgba(0,0,0,0.2);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .btn-panel:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 20px rgba(0,0,0,0.3);
        }

        /* Logo o icono opcional */
        .landing .logo {
            font-size: 4rem;
            margin-bottom: 1rem;
            color: #fff;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.5);
        }

        @media (max-width: 768px) {
            .landing h1 {
                font-size: 2.2rem;
            }
            .landing p {
                font-size: 1rem;
            }
            .btn-panel {
                font-size: 1.25rem;
                padding: 0.8rem 2rem;
            }
        }
    </style>
</head>
<body>

<div class="landing">
    <div class="logo">🚀</div>
    <h1>Bienvenido a Mi Aplicación</h1>
    <p>Tu panel de control personal para gestionar posts y usuarios de manera sencilla y moderna.</p>
    <a href="{{ path('posts_index') }}" class="btn btn-light btn-panel">
        Acceder al Panel
    </a>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

